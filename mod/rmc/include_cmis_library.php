<?php
include_once ($CFG->dirroot . '/mod/rmc/lib/cmis_repository_wrapper.php');
/* global $DB;
$repo = $DB->get_records('repository', array('type' => 'andresco_cmis'));
if(count($repo) > 0) { 
	include_once ($CFG->dirroot . '/repository/andresco_cmis/lib/cmis_repository_wrapper.php');
} else {
	include_once ($CFG->dirroot . '/mod/rmc/lib/cmis_repository_wrapper.php');
} */ 